# Public site URL — drives every canonical, OG, sitemap, JSON-LD on the site.
# Change this ONCE, rebuild, every URL on the site updates.
# No trailing slash.
VITE_SITE_URL="https://somritdasgupta.in"
